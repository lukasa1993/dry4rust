# dry4rust

Install with `cargo install --git https://github.com/lukasa1993/dry4rust --force`. Run `dry4rust --min-tokens 30 --fail`. Exit `2` means duplicate code remains. Keep tests excluded unless explicitly requested.
